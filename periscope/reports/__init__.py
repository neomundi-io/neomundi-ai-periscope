"""The Report Library.

`open/` contains the two report generators shipped in the public product:
Executive Snapshot and Model Release Benchmark Report.

`advanced/` documents additional report types (Full Metrology, Governance /
Review, FinOps, Longitudinal, Compliance Evidence, Custom) that are
available on request but not shipped as public generators in this build.

`common/` holds the rendering, charting, HTML/PDF and localization code
shared by every report, open or advanced.
"""
